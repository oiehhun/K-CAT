export class FirestoreStorageError extends Error {
    constructor(rawPath, ids, msg) {
        super(`Unable to get document from ${rawPath}. ${JSON.stringify(ids)} (${msg || ''})`);
        this.rawPath = rawPath;
        this.ids = ids;
    }
}
//# sourceMappingURL=error.js.map